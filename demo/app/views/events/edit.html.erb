<%= simple_form_for(@event) do |f| %>
  <%= simple_form_for setup_event(@event), :url => event_path(@event),
  :method => :put do |f| %>
    <%= render :partial => 'form', :locals => { :f => f} %>
    <%= f.submit "Update" %>
  <% end %>
<% end %>